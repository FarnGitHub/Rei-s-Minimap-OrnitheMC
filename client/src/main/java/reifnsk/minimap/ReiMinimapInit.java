package reifnsk.minimap;

import java.util.logging.LogManager;
import java.util.logging.Logger;

import net.ornithemc.osl.entrypoints.api.ModInitializer;
import net.ornithemc.osl.lifecycle.api.WorldEvents;
import net.ornithemc.osl.lifecycle.api.MinecraftEvents;
import reifnsk.minimap.mixin.MinecraftGetter;

public class ReiMinimapInit implements ModInitializer {

	// This logger is used to write text to the console and the log file.
	// It is considered best practice to use your mod name as the logger's name.
	// That way, it's clear which mod wrote info, warnings, and errors.

	public void init() {

	}
}
